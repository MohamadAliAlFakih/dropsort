# Licenses and third-party notices (dropsort)

## Project code

Application source in this repository is under the license stated in the root **LICENSE** file (`TODO(team):` confirm name and year if multiple authors).

## RVL-CDIP dataset (academic / research)

The Week 6 brief references the **RVL-CDIP** document image dataset for training and evaluation. Usage is subject to the dataset’s own terms (academic / research use). `TODO(team):` add the canonical citation and link as required by course staff.

**Disclaimer:** This file is for project documentation only and is not legal advice.

## Third-party software (non-exhaustive)

Backend and frontend depend on open-source packages listed in:

- `pyproject.toml` / `uv.lock`
- `frontend/package.json`

`TODO(team):` add notable direct dependencies (e.g. FastAPI, torch, MinIO client) if the course asks for explicit attribution tables.

## Container images

Compose and Dockerfiles reference upstream images (e.g. PostgreSQL, Redis, Vault, MinIO, SFTP, nginx). Each image is subject to its vendor’s license. `TODO(team):` optional table of image names and license URLs if required.

## Model weights

`TODO(team):` document license and provenance for shipped `classifier.pt` and `model_card.json` when artifacts are committed (including git LFS note if applicable).